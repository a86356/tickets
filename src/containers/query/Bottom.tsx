import React, {FC} from "react";
import './Bottom.css'

type Props={
}

const Bottom:FC<Props> = (props:Props):JSX.Element=>{

    return (
        <div className={"header"}>
            bottom
        </div>
    )
}

export default Bottom